# Cloudflare Workers static deploy (dashboard: build = npm run build, deploy = npx wrangler deploy)
# Project name must match Workers service: apnainternsite

name = "apnainternsite"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
