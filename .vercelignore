# Cloudflare Workers — not used by Vercel; avoid any framework/build detection noise.
wrangler.toml
wrangler.jsonc
cloudflare/
.wranglerignore

# API routes run on AWS Lambda (same as Cloudflare Workers proxy). Vercel serves static SPA only.
api/
aws/
